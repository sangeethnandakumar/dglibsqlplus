<idea-plugin>
    <id>com.sangeethnandakumar.dglibsqlplus</id>
    <name>DataGrip LibSQL Plus Driver</name>
    <vendor url="https://github.com/sangeethnandakumar/dglibsqlplus">Sangeeth Nandakumar</vendor>
    <description><![CDATA[
    <h2>Turso Remote Database Driver for DataGrip</h2>

    <p>Native JDBC driver that connects DataGrip to <b>libSQL</b> and <b>Turso</b> databases
    via the HTTP pipeline API. No external dependencies — just install and connect.</p>

    <h3>Features</h3>
    <ul>
        <li><b>Full schema introspection</b> — tables, columns, primary keys, indexes, and foreign keys in the database tree</li>
        <li><b>Proper query lifecycle</b> — no hanging spinners (fixes the DBeaver driver issue)</li>
        <li><b>SQLite dialect</b> — full SQL completion and syntax highlighting</li>
        <li><b>Turso Cloud ready</b> — paste your JWT auth token in the Password field</li>
        <li><b>Zero config</b> — auto-registers in DataGrip's driver list on install</li>
        <li><b>Clear error messages</b> — auth failures (401/403) surfaced with actionable guidance</li>
    </ul>

    <h3>Getting Started</h3>
    <ol>
        <li>Install this plugin and restart DataGrip</li>
        <li>Add a new Data Source → select <b>Turso Remote</b></li>
        <li>Enter your Turso database host (e.g. <code>my-db.turso.io</code>)</li>
        <li>Paste your Turso JWT auth token in the <b>Password</b> field</li>
        <li>Test Connection</li>
    </ol>

    <h3>Supported Databases</h3>
    <ul>
        <li>Turso Cloud databases</li>
        <li>Self-hosted libSQL servers (sqld)</li>
        <li>Local libSQL instances</li>
    </ul>

    <p>Source code: <a href="https://github.com/sangeethnandakumar/dglibsqlplus">github.com/sangeethnandakumar/dglibsqlplus</a></p>
    <p>Report issues: <a href="https://github.com/sangeethnandakumar/dglibsqlplus/issues">GitHub Issues</a></p>
    ]]></description>
    <change-notes><![CDATA[
    <b>1.0.0</b>
    <ul>
        <li>First stable release</li>
        <li>Auto-registers in DataGrip driver list — no manual JAR setup</li>
        <li>Full schema introspection: tables, columns, primary keys, indexes, foreign keys</li>
        <li>Auth token via Password field</li>
        <li>Fat JAR with shaded Gson for DataGrip's isolated driver classloader</li>
        <li>Clear error messages for auth failures (401/403)</li>
        <li>Custom keep-alive query support</li>
        <li>Turso illustrated head as plugin icon</li>
    </ul>
    ]]></change-notes>
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.database</depends>

    <extensions defaultExtensionNs="com.intellij.database">
        <driversConfig config="databaseDrivers/libsql-drivers.xml"/>
    </extensions>
</idea-plugin>
